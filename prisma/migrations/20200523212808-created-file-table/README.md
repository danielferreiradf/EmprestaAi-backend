# Migration `20200523212808-created-file-table`

This migration has been generated by danielferreiradf at 5/23/2020, 9:28:09 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."files" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"name" text  NOT NULL ,"path" integer  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."products" ADD COLUMN "pictureId" integer  NOT NULL ;

ALTER TABLE "public"."products" ADD FOREIGN KEY ("pictureId")REFERENCES "public"."files"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200518120415-added-created-at-updated-at-to-orders..20200523212808-created-file-table
--- datamodel.dml
+++ datamodel.dml
@@ -1,11 +1,12 @@
 generator client {
   provider = "prisma-client-js"
 }
+// DEV DB
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id             Int       @default(autoincrement()) @id
@@ -34,16 +35,29 @@
   description String
   location    String
   owner       User     @relation(fields: [ownerId], references: [id])
   ownerId     Int
+  picture     File     @relation(fields: [pictureId], references: [id])
+  pictureId   Int
   statusRent  Boolean  @default(false)
   createdAt   DateTime @default(now())
   updatedAt   DateTime @updatedAt
   Order       Order[]
   @@map("products")
 }
+model File {
+  id        Int       @default(autoincrement()) @id
+  name      String
+  path      Int
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  Product   Product[]
+
+  @@map("files")
+}
+
 model Order {
   id          Int      @default(autoincrement()) @id
   product     Product  @relation(fields: [productId], references: [id])
   productId   Int
```


